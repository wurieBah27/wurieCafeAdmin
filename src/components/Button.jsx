// const Button = ({ text }) => {
//   if (text === "Edit") {
//     return <button className="b">{Text}</button>;
//   }
//   return (
//     <button className={`bg-${bg}-200 py-[${py}] px-[${px}]`}>{text}</button>
//   );
// };

export default Button;
